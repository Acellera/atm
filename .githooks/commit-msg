# #!/usr/bin/env bash

# # regex to validate in commit msg
# commit_regex='(^FIX:|DOC:|FTR:|INT:)'
# error_msg="Aborting commit. Your commit message must start either with FIX: (for fixes), DOC: (for documentation changes), FTR (for new features): or INT: (for internal changes)"

# if ! grep -iqE "$commit_regex" "$1"; then
#     echo "$error_msg" >&2
#     exit 1
# fi